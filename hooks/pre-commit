#!/usr/bin/env bash
set -e

if ! command -v pytest &>/dev/null; then
    echo "Warning: pytest not found — skipping pre-commit tests"
    exit 0
fi

echo "Running fast tests (pre-commit)..."
PYTHONPATH=. pytest tests/test_models.py tests/test_deck.py -q
